---
// Above-the-fold company positioning. Software development is a current
// corporate activity; the four trade/homestead operating names remain staged
// future lines rather than active divisions.
import { Picture } from 'astro:assets';
import logoMedallion from '../assets/images/corporate/logo-medallion.webp';
---

<section class="section hero-section" id="hero" data-theme="corporate">
  <div class="background-layer">
    <div class="parallax-bg"></div>
    <div class="corporate-flux"></div>
  </div>

  <div class="content-layer">
    <div class="container">
      <p class="hero-status">
        <span class="hero-status-dot" aria-hidden="true"></span>
        Federally incorporated · Ontario · Software development active
      </p>

      <Picture
        class="hero-brand-mark"
        src={logoMedallion}
        alt="Fluxology, Inc. circular flux-wave emblem"
        formats={['avif', 'webp']}
        fallbackFormat="webp"
        widths={[160, 320, 440]}
        width={440}
        sizes="(max-width: 767px) 140px, 220px"
        loading="eager"
        decoding="async"
        fetchpriority="high"
      />
      <h1 class="hero-title">
        <span class="hero-title-main">Fluxology, Inc.</span>
        <span class="hero-title-sub">Independent software. Trade capability. Long-term resilience.</span>
      </h1>

      <p class="hero-description">
        A one-owner Canadian-controlled private corporation developing and publishing its own software products now,
        with staged future operating lines in fabrication, digital production support and homestead food systems.
      </p>

      <ul class="hero-facts" role="list">
        <li><strong>Now</strong><span>software product development</span></li>
        <li><strong>iOS</strong><span>applications in active development</span></li>
        <li><strong>2029+</strong><span>earliest trade-line launch</span></li>
      </ul>

      <div class="hero-cta-group">
        <a href="/apps/" class="cta-button cta-primary">Software & Apps</a>
        <a href="#operating-model" class="cta-button cta-secondary">See the full operating model</a>
      </div>

      <div class="hero-scroll-indicator" aria-hidden="true">
        <span class="scroll-arrow">↓</span>
        <span class="scroll-text">Explore Fluxology</span>
      </div>
    </div>
  </div>
</section>

<style>
  picture {
    display: contents;
  }

  .hero-status {
    display: flex;
    gap: var(--spacing-sm);
    justify-content: center;
    align-items: center;
    width: fit-content;
    margin: 0 auto var(--spacing-lg);
    padding: var(--spacing-sm) var(--spacing-lg);
    border: 1px solid rgba(168, 218, 220, 0.28);
    border-radius: var(--radius-full);
    background: rgba(9, 30, 45, 0.58);
    color: rgba(255, 255, 255, 0.78);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    letter-spacing: var(--letter-spacing-wide);
    text-transform: uppercase;
  }

  .hero-status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--corporate-accent-blue);
    box-shadow: 0 0 0 5px rgba(58, 134, 255, 0.13);
  }

  .hero-facts {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--spacing-md);
    max-width: 760px;
    margin: calc(var(--spacing-xl) * -1) auto var(--spacing-2xl);
    padding: 0;
    list-style: none;
  }

  .hero-facts li {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--spacing-md);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: var(--radius-lg);
    background: rgba(9, 30, 45, 0.38);
    text-align: center;
    backdrop-filter: blur(8px);
  }

  .hero-facts strong {
    color: var(--corporate-accent-silver);
    font-family: var(--font-corporate-heading);
    font-size: var(--font-size-lg);
  }

  .hero-facts span {
    color: rgba(255, 255, 255, 0.68);
    font-size: var(--font-size-xs);
  }

  @media (max-width: 767px) {
    .hero-status {
      max-width: 100%;
      text-align: center;
    }

    .hero-facts {
      grid-template-columns: 1fr;
      gap: var(--spacing-sm);
      margin-top: 0;
    }

    .hero-facts li {
      display: grid;
      grid-template-columns: 76px minmax(0, 1fr);
      text-align: left;
    }

    .hero-facts strong {
      font-size: var(--font-size-md);
    }
  }
</style>
