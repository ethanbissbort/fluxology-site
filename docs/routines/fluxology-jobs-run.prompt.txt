Execute the Fluxology Jobs scheduled run (T176 trades job scout). Fresh session each 12h fire, zero prior context.

1. BOOTSTRAP THE REPO FIRST — this scheduled session starts with NO repository attached; nothing below works until this succeeds. (a) Use ToolSearch to load the claude-code-remote MCP tools if they are deferred, then call add_repo with {owner: "ethanbissbort", repo: "fluxology-site", access: "push"}. Call it directly — do NOT pre-check with git ls-remote, gh, or curl (unauthenticated probes return misleading 404s). (b) Run the clone command returned by add_repo, cloning into the session working directory, then call register_repo_root with the clone's absolute path so the repo's skills load. (c) The default branch (main) of the fresh clone contains everything needed, including .claude/skills/ — no branch recovery is required. If add_repo or the clone fails, retry up to 3 times with exponential backoff (2s/4s/8s); if it still fails, stop research and make your completion report state the exact error and that the run could not access the repository. Never fabricate repository content or coverage.

2. Load the full spec: .claude/skills/fluxology-jobs-run/SKILL.md plus references/ (search-profile.md, source-strategy.md, ranking-rubric.md), all on main. Follow it exactly: separate lane passes (welding/fabrication, plumbing/piping, machine shop, metal finishing, industrial labour, adjacent), Semester 1 welding+plumbing weighting, hard-gate filtering distinguishing required vs preferred, dedup before ranking (prefer employer-direct; mark reposts repost, never newly discovered), verdicts APPLY / APPLY — STRATEGIC STRETCH (stored enum STRETCH; rationale mandatory) / MAYBE / SKIP with 0-10 T176 fit scores and labelled shortlist-chance ranges.

3. Sources: use the Indeed MCP tools when attached (they should be, via this routine's connector grant); ToolSearch-probe ("select:mcp__Indeed__search_jobs") before declaring any tool unavailable — an unloaded schema is not absence. If Indeed is truly absent, say so plainly with the exact coverage impact. Regardless, ALWAYS run employer-direct and apprenticeship-channel discovery via WebSearch/WebFetch (union/mechanical contractors, fab shops, George Brown-adjacent channels). Never claim a source was searched that was not invoked.

4. Write path, graceful degradation. ToolSearch-probe for dashboard.feed/dashboard.upsert (or upsert_job_listings). If present: dashboard.feed {dashboard:"jobs", mode:"ids"} to diff, then one dashboard.upsert {dashboard:"jobs", listings:[...]}; timestamps are server-owned — do not author them; skip the fallback. If absent (expected headless): diff against the live feed https://jobs.fluxology.ca/data/listings.json via WebFetch (repo copy = bootstrap snapshot only), then ONE merge-by-id commit of public/jobs/data/listings.json to main via GitHub MCP create_or_update_file — upsert semantics, never full-feed replacement; preserve root metadata (schemaVersion 3, appVersion, generatedAt, searchName, currency) and every unrelated record; author firstSeen/lastVerified/lastChanged on this path only; SKIP the commit when nothing materially changed. Records: the SKILL.md convention PLUS the schema/frontend fields it omits — company (=employer, schema-required), tracks/primaryTrack (from lane), fitScore (=careerFitScore x10, 0-100), salaryText (=pay). Ids stable: indeed-<jobKey> else job-<slug-of-employer-title>. Never delete — retire with active:false. Never inject browser-owned workflow state (Saved/Applied/...) or personal notes.

5. Hard limits: the jobs feed is the only write surface — no PRs, no branches beyond the main fallback commit, no email, no applications or employer contact; never log tokens.

6. NO-SILENCE: end with the complete user-visible report even with zero results — market pattern across sources; shortlist (5-8 roles max, fewer beats filler; each with all 14 SKILL.md output fields incl. exact URL) or explicit zero-qualifying statement per lane; coverage audit with exact counts (queries per lane per source, results inspected, postings opened/verified, duplicates collapsed, records upserted); every missing capability and its coverage impact.