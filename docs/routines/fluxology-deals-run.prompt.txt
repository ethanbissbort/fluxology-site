Fluxology Deals scheduled run: LEGO deal watch, eBay Track A + Kijiji Track B.

1. REPO CHECK — the fluxology-site repository is attached to this scheduled session as an environment source and should already be cloned in the working directory (e.g. /home/user/fluxology-site). Verify the checkout and freshen it: git fetch origin main && git checkout main && git reset --hard origin/main. If the checkout is missing, clone it directly: git clone https://github.com/ethanbissbort/fluxology-site.git (retry failures up to 3 times with 2s/4s/8s backoff). The skill files are in .claude/skills/ on main. If the repository still cannot be read, stop research and make your completion report state the exact error. Never fabricate repository content or coverage.

2. Read .claude/skills/fluxology-deals-run/SKILL.md in the clone — the normative spec; follow its OPERATIONS mapping and TASK SPECIFICATION exactly: two independently audited tracks (A: eBay.ca ship-to M6H 2W9; B: Kijiji local); watched-seller drill-downs through all inventory exposed (never first-page-only), same-run drill-down + watch-list decision for new good sellers; canonical validation — open the live eBay /itm/ page or exact Kijiji ad before surfacing, never from snippets; independent 45 km AND 65 km Kijiji passes deduplicated by stable id/URL; its taxonomy, valuation, shipping-honesty, exclusion, and manual-validation-candidate rules (1-4 failed checks listed with canonical URLs; 5+ summarized).

3. Browsing needs the Browser Bridge connector — usually ABSENT here (not grantable to this routine). Detect via ToolSearch — connector tools appear as mcp__<connector>__..., not dotted names. If present: browser.session_open (specific deviceId, or "default" only when exactly one PC is online) -> browser.navigate -> browser.extract (candidates on eBay /sch/+/str/|/usr/ and Kijiji /b-* pages with hasNextPage/nextPageUrl; full records on item pages). Inspect everything exposed; never stop early; on rate limits slow and retry, never silently abandon.

4. Writes: dashboard.feed {dashboard:"deals", mode:"ids"} first to diff, then dashboard.upsert {dashboard:"deals", listings:[...]} for new/materially-changed records ONLY; ids ebay-<itemId>/kijiji-<adId>. Match public/deals/data/listings.json + schema.json (v3): category 'LEGO Minifigures' or 'LEGO Specialty Parts' (incomplete sets: existing conventions; never rename other categories); per-record searchName 'LEGO Specialty & Minifig Watch' (never overwrite the ROOT searchName); active=true only on direct-validated records.

5. If those tools are absent: one merge-by-id commit of public/deals/data/listings.json made locally on main and pushed with git push origin main — this instruction is your explicit permission to push the feed commit directly to main; never use the session's designated claude/... outcome branch for feed commits; retry the push up to 4 times with 2s/4s/8s/16s backoff on network errors; never force-push. Merge with the clone's current file; preserve root metadata, every unrelated/historical record, and the one-record-per-line format; lastSeen only when checked, lastChanged only on material change; skip when nothing materially changed. Never create a PR, push another branch, or send email.

6. Browser Bridge absent or no Windows device online: fabricate nothing — state the missing capability and exact coverage impact, do what does not need it, still report in full.

7. Hard limits: read-only research — never bid, buy, offer, message a seller, or mutate watchlists/carts. Credentials never appear in task text or output.

8. NO-SILENCE — the run is the report; end with the complete report: exact-count coverage audits for BOTH tracks per SKILL.md (A: broad-search and per-seller inspected, canonical pages opened, evaluations, surfaced, exclusions by reason, caps/failures with exact counts; B: 45+65 km inspected, overlap dupes removed, pages opened, evaluations, surfaced, exclusions, failures), surfaced deals or an explicit zero-qualifying statement per track, and all manual-validation candidates. Never fabricate counts.
