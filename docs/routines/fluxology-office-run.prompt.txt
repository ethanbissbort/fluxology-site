Execute the Fluxology Office scheduled run — the GTA office-market search for Fluxology Inc. This fires every 12h (UTC cron 0 0,12 * * *) into a fresh cloud session with zero prior context.

1. REPO CHECK — the fluxology-site repository is attached to this scheduled session as an environment source and should already be cloned in the working directory (e.g. /home/user/fluxology-site). Verify the checkout and freshen it: git fetch origin main && git checkout main && git reset --hard origin/main. If the checkout is missing, clone it directly: git clone https://github.com/ethanbissbort/fluxology-site.git (retry failures up to 3 times with 2s/4s/8s backoff). The skill files are in .claude/skills/ on main. If the repository still cannot be read, stop research and make your completion report state the exact error. Never fabricate repository content or coverage.

2. Load the normative spec: read .claude/skills/fluxology-office-run/SKILL.md from the fresh clone (the skill files are on main). Follow its OPERATIONS mapping and TASK SPECIFICATION exactly — the two SEPARATE deliverables (A: non-managed private offices; B: managed-office providers; never merged), the private-office gate (desks/coworking never qualify), 24/7 access vs 24/7 HVAC as separate fields, the provider watchlist, the 100-point Managed Office Value Score with its exact weights, the four price figures, the outreach-queue record contents, the exact-count coverage audit, and the CRITICAL LINK RULE (only exact listing URLs verified live THIS RUN).

3. Research primarily via WebSearch/WebFetch on provider sites, GTA location/pricing pages, and listing sources. The Browser Bridge MCP (browser.navigate/snapshot/extract) is optional and usually ABSENT in headless runs — if absent (or no Windows agent online), do not hunt for it: use WebSearch/WebFetch only and state the exact coverage impact in the audit (e.g., Kijiji/JS-heavy pages).

4. Dashboard writes — choose by what is actually attached: (a) Browser Bridge: dashboard.feed {dashboard:"office", mode:"ids"} to diff, then dashboard.upsert {dashboard:"office", listings:[...]}; or (b) fluxology-mcp: upsert_office_listings. On either direct path the server owns priceHistory/firstSeen/lastSeen/lastVerified/lastChanged — do not send them. (c) If NO direct write tool is attached (typical headless), fall back to ONE merge-by-id commit of public/office-scout/data/listings.json made locally on main and pushed with git push origin main — this instruction is your explicit permission to push feed commits directly to main; never the session's designated claude/... outcome branch; retry the push up to 4 times with 2s/4s/8s/16s backoff on network errors; never force-push. Merge by stable id; preserve root metadata (schemaVersion 2.5, hardAllInCeilingCad) and EVERY unrelated or old record; retire only via active:false; append dated priceHistory, never overwrite history; costStatus "verified" only when ALL mandatory recurring costs are known and included. Provider profiles live separately in managed-providers.json (schemaVersion 1, providers[] + scoreModel — keep weights in sync); include it in the same commit if changed, noting the sync workflow pushes only listings.json to the live API. Never run the fallback after a successful direct write; skip commits when nothing materially changed. No PRs; push to no other branch.

5. EMAIL: never send email. Outreach output is OUTREACH QUEUE records plus fully drafted message text in the report; sending happens later through the mail-mcp human-approval workflow (docs/EMAIL-WORKFLOW.md).

6. Degraded runs: if a needed capability is missing (bridge absent, no direct dashboard tool, a site blocking fetches), complete what is possible and state exactly what was missing and the coverage impact — never fabricate coverage or claim a source was searched when it was not.

7. NO-SILENCE: your final message must be the complete user-visible completion report with BOTH deliverables, the outreach queue, and the full exact-count coverage audit, even if no worthwhile new office was found.
