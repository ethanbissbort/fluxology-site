# Virtual Host Configuration for Fluxology Website
# ==============================================

# HTTP Virtual Host
<VirtualHost *:80>
    ServerName localhost
    ServerAdmin admin@fluxology.ca

    DocumentRoot "/usr/local/apache2/htdocs"

    <Directory "/usr/local/apache2/htdocs">
        Options -Indexes +FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    # Branded 404 built by Astro (no SPA rewrite — static site)
    ErrorDocument 404 /404.html

    # Logging
    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined

    # Security Headers
    <IfModule mod_headers.c>
        Header always set X-Frame-Options "SAMEORIGIN"
        Header always set X-Content-Type-Options "nosniff"
        Header always set X-XSS-Protection "0"
        Header always set Referrer-Policy "strict-origin-when-cross-origin"
    </IfModule>
</VirtualHost>

# HTTPS Virtual Host (uncomment when SSL certificates are configured)
# <VirtualHost *:443>
#     ServerName fluxology.ca
#     ServerAlias www.fluxology.ca
#     ServerAdmin admin@fluxology.ca
#
#     DocumentRoot "/usr/local/apache2/htdocs"
#
#     # SSL Configuration
#     SSLEngine on
#     SSLCertificateFile /etc/ssl/certs/fluxology.crt
#     SSLCertificateKeyFile /etc/ssl/private/fluxology.key
#     SSLCertificateChainFile /etc/ssl/certs/ca-bundle.crt
#
#     # Strong SSL Configuration
#     SSLProtocol all -SSLv3 -TLSv1 -TLSv1.1
#     SSLCipherSuite HIGH:!aNULL:!MD5:!3DES
#     SSLHonorCipherOrder on
#     SSLCompression off
#     SSLSessionTickets off
#
#     <Directory "/usr/local/apache2/htdocs">
#         Options -Indexes +FollowSymLinks
#         AllowOverride None
#         Require all granted
#     </Directory>
#
#     ErrorDocument 404 /404.html
#
#     # Logging
#     ErrorLog /proc/self/fd/2
#     CustomLog /proc/self/fd/1 combined
#
#     # HSTS Header (kept consistent with the active policy: X-XSS-Protection
#     # is deliberately "0", and HSTS omits preload — see httpd.conf)
#     <IfModule mod_headers.c>
#         Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
#         Header always set X-Frame-Options "SAMEORIGIN"
#         Header always set X-Content-Type-Options "nosniff"
#         Header always set X-XSS-Protection "0"
#         Header always set Referrer-Policy "strict-origin-when-cross-origin"
#     </IfModule>
# </VirtualHost>

# Redirect HTTP to HTTPS (uncomment when SSL is configured)
# <VirtualHost *:80>
#     ServerName fluxology.ca
#     ServerAlias www.fluxology.ca
#
#     RewriteEngine On
#     RewriteCond %{HTTPS} off
#     RewriteRule ^(.*)$ https://%{HTTP_HOST}$1 [R=301,L]
# </VirtualHost>
